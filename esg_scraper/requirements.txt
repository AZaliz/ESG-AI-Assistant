requests>=2.31
beautifulsoup4>=4.12
# brotli decoding — required for sending Accept-Encoding: br in browser-mimic headers.
# Without it, sites like BNP Paribas and Schneider Electric return 403 because
# the WAF treats "no br support" as a bot signal.
brotli>=1.1.0

# --- parser/ stage (build_index, parser, table_normalizer, chunker) ---
# pdfplumber: text+table extraction per page. table_normalizer depends on its
# table detection — text-only extraction misses ~75% of KPI figures.
pandas>=2.2
pdfplumber>=0.11
# tabulate: pandas .to_markdown() backend for build_report.md. Without it the
# final report write crashes (chunks.jsonl/csv are already safe by then).
tabulate>=0.9
