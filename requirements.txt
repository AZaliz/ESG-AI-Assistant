beautifulsoup4>=4.12
# brotli decoding — required for sending Accept-Encoding: br in browser-mimic headers.
# Without it, some sites return 403 because the WAF treats "no br support" as a bot signal.
brotli>=1.1.0
faiss-cpu>=1.13
lxml>=5.2
numpy>=1.26
pydantic>=2.8
rapidfuzz>=3.9
requests>=2.32
tenacity>=9.0
PyMuPDF>=1.24
pytest>=8.3
streamlit>=1.38
